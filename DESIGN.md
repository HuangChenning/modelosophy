# 企业情报报告 · 视觉与版式规范（DESIGN.md）

适用范围：本仓库内所有 Skill 输出的 HTML 报告类产物的通用视觉与版式规范。当前唯一参考实现为 `sales-company-intel-report` 技能输出的 `report.html`。
文件对应关系（以 `sales-company-intel-report` 为例）：`assets/report_template.html`（Jinja2 模板，含全部样式与脚本）+ `scripts/generate_report.py`（数据准备，模板内不做任何格式化计算）。

设计目标：一份可屏幕阅读、也可直接打印成 A4 的咨询报告。结论先行、事实与推断可区分、拜访前 10 分钟能读完。

---

## 1. 设计原则

1. **结论先行**：封面 → 关键指标带 → 执行摘要 → 分章正文。读者在第一屏内应看到「有多少条招投标记录、金额多少、还有多少事没核实」。
2. **克制的视觉**：白底、深藏青墨色、单一蓝色强调色。不使用渐变、阴影、圆角卡片、emoji、装饰性插图。
3. **线条承担结构**：分区靠 1px / 1.5px / 2px 三档规则线与留白，不靠色块。色块只用于极少数强调（执行摘要底色、图表柱体、序号方块）。
4. **数据可追溯**：每条事实带置信度标签与来源编号 `[S1]`，可点击跳转到参考来源章节。
5. **不泄露内部结构**：正文中不出现 `data.json` 的字段名（`vendor_summary` 等），由 `scrub_fields()` 自动替换为中文说法。

---

## 2. 色彩

| Token | 值 | 用途 |
|---|---|---|
| `--bg` | `#ffffff` | 页面底色 |
| `--paper` | `#f5f7f9` | 侧边栏、注释块、清单块底色 |
| `--ink` | `#0b2238` | 主文字、标题、粗分隔线 |
| `--ink-2` | `#46586b` | 次级正文、说明文字 |
| `--muted` | `#7b8b9a` | 标签、图注、来源说明 |
| `--rule` | `#d5dde4` | 常规分隔线（1px） |
| `--rule-soft` | `#e8edf1` | 表格行线、网格内线 |
| `--accent` | `#1957a8` | 唯一强调色：章节序号、链接、图表柱体、强调竖条 |
| `--accent-deep` | `#0f3f7a` | 链接 hover |
| `--accent-lite` | `#eaf1f9` | 执行摘要底色 |

置信度三色（仅用于标签与 SWOT 小标题，不做大面积填充）：

| 状态 | 文字/描边 | 底色 |
|---|---|---|
| 已证实 | `#1d6b45` | `#ecf4ef` |
| 推断 | `#9a6408` | `#fdf3e3` |
| 待核实 | `#a32c22` | `#fbecea` |

**规则**：强调色只有一种；不引入第二套品牌色；柱状图为纯色 `--accent`，多系列折线用 `--accent` + `#8aa6c8` 区分（第二条虚线）。

---

## 3. 字体与字号

字体栈（无衬线，中英文混排）：
`"Helvetica Neue", Helvetica, Arial, "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif`

不加载任何 Web Font，避免离线环境失效。

| 元素 | 字号 | 字重 | 备注 |
|---|---|---|---|
| 正文 | 14px / 1.62 | 400 | |
| 封面主标题 | `clamp(26px, 3vw, 34px)` | 700 | 最宽 28ch |
| 章节标题 | 19px | 700 | 章节序号 17px，与标题近乎齐平 |
| 小节标题 `h3.h` | 12px | 700 | 全大写、字距 0.09em、下方 1px 线 |
| 执行摘要 headline | 13.5px / 1.65 | 400 | 不加粗，宽度与正文一致 |
| 摘要要点 | 13.5px | 400 | 序号为 18×18 蓝色方块 |
| 表格 | 12.5px | 400 | 表头 10px / 700 / 全大写 |
| KPI 数字 | 25px | 700 | 单位 12px |
| 图注、来源 | 10.5–11.5px | 400 | `--muted` |

**数字**：所有金额、年份、编号使用 `font-variant-numeric: tabular-nums`，金额右对齐。
**换行**：置信度列、金额列、类别列、键值标签列一律 `white-space: nowrap`，避免短内容被拆行。

---

## 4. 版式与栅格

- 外层为 `flex`：左侧 252px 常驻导航栏（`position: sticky`，满屏高），右侧内容区。
- 内容区最大宽度 1060px，内边距 `40px 52px 80px`。
- 正文段落不设独立限宽，与表格、图表同宽。
- 断点 1000px：导航栏转为顶部横条，目录两栏；620px：SWOT、来源列表转单栏。

**分隔层级**
- 2px 深墨线：封面底部、图表上沿、页脚上沿（最强分隔）
- 1.5px 深墨线：表头下沿
- 1px `--rule`：章节之间、小节标题下沿
- 1px `--rule-soft`：表格行、网格内线

---

## 5. 组件规范

**封面**：顶部 56×4px 蓝色标记条 → 全大写英文 eyebrow → 主标题 → 副标题 → 元信息网格（所属行业 / 报告用途 / 检索截止）→ 范围声明（浅灰底、左侧蓝色竖条）。

**关键指标带（KPI）**：顶部 3px 深墨线的等分网格，最多 5 格。全部由数据算出（招投标记录数、金额合计、已确认中标金额、待核实条数、来源数），缺数据的格子不渲染，不允许估算或占位。

**执行摘要**：headline 为浅蓝底 + 左侧 3px 蓝色竖条的整段结论；要点为编号列表（蓝色方块序号）；末尾为「首次接触必须核实」清单（浅灰底、顶部 3px 蓝线）。

**章节**：`序号 + 标题 + 折叠箭头`，点击标题行折叠/展开，键盘可操作。侧边栏提供「全部折叠 / 全部展开」。打印时强制展开。

**表格**：无竖线、无斑马纹；表头深墨下边框；行 hover 浅灰。首列（项目名/年份/角色）加 `.k` 表示为标签列，不换行。带 `data-sort` 的表头可点击排序（数字按数值、文本按中文拼音）。招投标表另有搜索框与「命中 / 总数」计数。

**置信度标签**：小圆点 + 文字的描边胶囊，2px 圆角，不换行。图例常驻侧边栏。

**图表（Exhibit）**：上沿 2px 深墨线，标题自动编号「图表 N | 标题」，底部为资料来源行。三种图型均为模板内置的内联 SVG，无外部依赖（不使用 Chart.js/CDN），保证离线可用、打印清晰。
- 纵向柱状：招投标金额时间线，类目标签旋转 35°，下方留白按最长标签自动计算，项目名称完整展示。
- 横向柱状：供应商中标金额，类目在左，数值在右端。
- 折线：财报趋势，第二条系列用浅蓝虚线，末点标注数值。
- 金额轴按量级自动换算为「元 / 万元 / 亿元」，单位标在轴端。

**决策链图**：SVG 节点框按层级居中排布，节点坐标由 Python 预计算。节点填色表示态度倾向（开放 / 谨慎 / 中立 / 信息不足），配图例。

**SWOT**：2×2 网格，小标题全大写 + 英文副标，四格颜色分别取已证实 / 待核实 / 强调蓝 / 推断色。

**待核实清单**：编号（蓝色描边方块）+ 问题 + 优先级标签（高＝实心红，中＝描边黄，低＝灰）+ 影响说明 + 建议渠道。

**参考来源**：双栏，编号蓝色，正文中的 `[S1]` 可跳转，跳转目标高亮。

---

## 6. 打印 / PDF

- `@page { size: A4; margin: 15mm 14mm 13mm; }`，正文 9.5pt。
- 隐藏侧边栏、搜索排序工具、折叠箭头；折叠状态强制展开。
- 封面单独成页；标题不与其后内容分离（`break-after: avoid`）；表格 `thead` 跨页重复；图表、KPI、SWOT 单元、问题条目不跨页断开。
- 图表高度压到 200px；链接去色，来源 URL 保留可读。

---

## 7. 交互（打印时全部降级）

| 交互 | 说明 |
|---|---|
| 阅读进度 | 侧边栏 3px 进度条 + 百分比 |
| 当前章节高亮 | 滚动时侧边栏对应项加粗、左侧蓝色标记 |
| 章节折叠 | 点击标题行；侧边栏可一键全部折叠/展开 |
| 表格排序 | 任何带 `data-sort` 表头的表格 |
| 招投标搜索 | 跨项目名 / 供应商 / 类别 / 时间的即时过滤 |
| 打印按钮 | 侧边栏「打印 / PDF」 |

---

## 8. 内容规则

1. 章节顺序：执行摘要 01 → 公司概况 02 → 业务与IT架构分层 03 → 战略方向 04 → IT现状与投入 05 → 近3年IT招投标 06 → 决策链与高管 07 → 客户侧SWOT 08 → 待核实清单 09 → 参考来源 10。
2. 章节导语只写「这一章回答什么问题」，不罗列字段名；无话可说时不写。
3. 长段落必须结构化：采购趋势分析按「小标题：内容」拆成分行（预算趋势 / 技术选型趋势 / 供应商集中度 / 招采节奏 / 特别说明），由 `split_trend_analysis()` 自动完成。
4. 没有数据时输出明确的空态说明（「公开渠道未检索到…」），不得留空、不得用行业均值臆造数字。
5. 所有格式化（金额千分位、份额百分比、类别拼接、SVG 坐标、KPI）都在 `generate_report.py` 完成，模板内只做 `{% if %}` / `{% for %}` 与取值。
6. 报告范围严格限定为客户客观情况，不含产品匹配、竞品对比、报价、销售话术。
